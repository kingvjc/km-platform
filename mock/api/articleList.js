module.exports = function(req, res) {
    return {
        status: 0,
        message: 'success',
        list: [{
        	title: '数据库字段varchar和char的区别',
        	article: `首先动态创建一个容器标签元素，如DIV，然后将要转换的字符串设置为这个元素的innerText(ie支持)或者textContent(火狐，google支持)，最后返回这个元素的innerHTML，即得到经过HTML编码转换的字符串了动态创建一个容器标签元素，如DIV，然后将要转换的字符串先动态创建一个容器标签元素，如DIV，然后将要转换的字符串设置为这个元素的innerText(ie支持)或者textContent(火狐，google支持)，最后返回这个元素的innerHTML，即得到经过HTML编码转换的字符串了动态创建一个容器标签元素，如DIV，然后将要转换的字`
        }],
        total: 1
    };
};